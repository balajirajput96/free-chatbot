# Browser smoke test

The local `index.html` rendered successfully in Chromium at `file:///home/ubuntu/github-audit/free-chatbot/index.html`.

Observed UI elements: privacy badge, local assistant status, Export and Clear controls, message textarea, Send button, prompt chips, and responsive two-column layout.

The initial greeting appeared in Hindi. Submitting the `help` prompt produced the expected local rule-based response explaining supported capabilities and the offline/private boundary. No login, API key, external request, or account access was used.
